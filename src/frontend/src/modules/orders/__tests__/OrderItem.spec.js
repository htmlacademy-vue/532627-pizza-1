import OrderItem from "@/modules/orders/OrderItem";

describe("OrderItem", () => {
  test("render order id", () =>{});
  test("render order total", () =>{});
  test("repeat order", () =>{});
  test("render order composition", () =>{});
  test("render order address", () =>{});
});
